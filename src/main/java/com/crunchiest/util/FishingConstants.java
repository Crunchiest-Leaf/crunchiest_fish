package com.crunchiest;

/**
 * A utility class that holds constants related to fishing mechanics.
 */
public class FishingConstants {
    
    /** The minimum reel time in milliseconds. */
    public static final int MIN_REEL_TIME_MS = 5000;

    /** The maximum reel time in milliseconds. */
    public static final int MAX_REEL_TIME_MS = 8000;

    /** The minimum target clicks required to reel in a fish. */
    public static final int MIN_TARGET_CLICKS = 10;

    /** The maximum target clicks required to reel in a fish. */
    public static final int MAX_TARGET_CLICKS = 50;
}